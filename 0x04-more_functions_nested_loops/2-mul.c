#include "main.h"
/**
 * mul - multiplies two integers.
 * Return: the result of multiplication.
 * @a : the integer to multiplie.
 * @b : the integer to multiplie.
 */
int mul(int a, int b)
{
return ((a * b));
}
