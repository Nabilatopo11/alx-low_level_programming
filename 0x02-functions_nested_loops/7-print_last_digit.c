#include "main.h"
/**
* print_last_digit -print last digit of n.
* @n : is the int to calculate the last digit.
* Return: the last digit.
*/
int print_last_digit(int n)
{
int lastdigit = n % 10;
return(lastdigit);
}
